{
	"name": "alumnlink",
	"version": "1.0.0",
	"description": "AlumnLink - Alumni Management Platform",
	"scripts": {
		"install:all": "npm install && npm install --prefix backend && npm install --prefix client",
		"start": "npm start --prefix backend",
		"dev:backend": "npm run dev --prefix backend",
		"dev:frontend": "npm run dev --prefix client",
		"dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
		"build": "npm install --prefix backend && npm install --prefix client && npm run build --prefix client"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"concurrently": "^8.2.0"
	}
}
